imports:
    - { resource: doctrine.yaml }

framework:
    rate_limiter:
        price_change_alarm_throttling:
            policy: 'fixed_window'
            limit: 1
            interval: '1 hour'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\Screener\:
        resource: '../../../'
